---
type: 세션보고서
Phase: 1 — 도구·권한·승인
대상 TODO: ①②③
날짜: 2026-08-20
브랜치: hwangjaewon/day3-consult-agent
---

# Phase 1 세션 보고서 — TODO ①②③

## 구현 내용

### TODO ① `tools/OrderTools.java` — 완료 기준 1·2

- `@Tool` description: "주문 상태를 조회한다. 사용자가 주문번호를 말하거나 '내 주문', '배송 언제 와요'
  처럼 자신의 주문 상태·배송 현황을 물으면 이 도구를 쓴다." — 언제 쓰는지 + 예시 표현 포함.
- `@ToolParam(description = "주문번호. 예: 12345")`.
- `userId`는 파라미터가 아니라 `ToolContext.getContext().get("userId")`로만 받는다 —
  모델이 바꿔 부를 수 없는 통로.
- 조회는 `OrderRepository.findByIdAndOwnerId(orderId, userId)`(완성 코드) 하나만 사용 —
  소유자 검증을 빠뜨릴 수 없는 구조.
- 없거나 남의 주문이면 **완전히 동일한 문구** `"해당 주문을 찾을 수 없습니다."`를 반환한다
  (예외 던지지 않음, 403 아님) — 존재 여부 자체를 노출하지 않기 위해서다.

### TODO ② `tools/RefundTools.java` — 완료 기준 3

- description에 "이 도구는 접수까지만 하며 즉시 처리되지 않는다 — 담당자 승인 후에 처리된다"를
  명시해 모델이 "내가 바로 처리했다"고 답하지 않게 했다.
- 권한을 `findByIdAndOwnerId`로 **먼저** 확인하고, 통과 시에만 `TicketRepository.create()`를
  호출한다(PENDING 상태로만 접수). `TicketRepository.approve()`는 이 파일에서 절대 호출하지 않는다
  — 승인 경로는 `AdminController`(도구 목록 밖)에만 있다.
- 반환 문구에 "접수"와 티켓 번호를 포함한다.

### TODO ③ `audit/ToolAuditAspect.java` — 완료 기준 7

- `@Around("@annotation(org.springframework.ai.tool.annotation.Tool)")`로 `@Tool` 메서드 전체를
  가로챈다(도구별로 로깅 코드를 심지 않아도 빠짐없이 잡힌다).
- 도구명·userId(`ToolContext`에서 추출)·인자·결과·소요시간(ms)을 `AI_TOOL_AUDIT` 로거로 남긴다.
- 정규식으로 주민등록번호(`\d{6}-\d{7}`)·카드번호(`\d{4}-\d{4}-\d{4}-\d{4}`)·이메일 형태를
  인자·결과 문자열 양쪽에서 마스킹한다.
- 예외 발생 시 `status=FAIL`로 경고 로그를 남기고 **다시 던진다**(대화 전체를 막지 않는다).

## 검증 결과

`./gradlew test --tests '*OrderToolsTest' --tests '*RefundToolsTest'` → **BUILD SUCCESSFUL**, 5건 전부 통과.

```
OrderToolsTest (3건)
  본인_주문은_조회된다() — PASS
  남의_주문은_조회되지_않는다() — PASS
  존재하지_않는_주문도_같은_문구다() — PASS

RefundToolsTest (2건)
  환불은_접수까지만_된다() — PASS
  남의_주문은_환불_접수도_안된다() — PASS
```

전체 스위트(`./gradlew test`, 11건): **8건 통과 / 3건 실패**.
실패 3건은 전부 `ConversationIdTest`(TODO ⑦ 대상, 계획대로 아직 미착수) — 예정된 실패다.

TODO ③(감사 Aspect)은 전용 단위 테스트가 없다 — README 안내대로 Phase 2에서 `bootRun` 후
실제 도구 호출 로그로 `AI_TOOL_AUDIT` 라인을 눈으로 확인할 예정이다.

## 막힌 지점

없음 — 계획대로 순조롭게 진행됨.

## 다음 단계

Phase 2(TODO ④⑤⑥) 진입 — `OPENAI_API_KEY`가 필요한 시점. 사용자가 직접 export 후 신호를 주면
`bootRun`으로 실제 모델 호출 검증을 시작한다.
